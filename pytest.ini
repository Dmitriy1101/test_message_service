[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = tests.py test_*.py *_tests.py
pythonpath = . core/
addopts = --tb=short 
; -v --no-header --no-summary -q
; filterwarnings =
;     ignore::DeprecationWarning
;     ignore::PendingDeprecationWarning
; asyncio_mode = auto
